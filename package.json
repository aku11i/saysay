{
  "name": "saysay",
  "version": "0.0.1",
  "description": "",
  "main": "main.cjs",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "dev": "node --loader ts-node/esm/transpile-only dev.ts",
    "prebuild": "yarn clean",
    "build": "npm-run-all build:*",
    "build:esbuild": "esbuild --target=node14 --format=cjs --out-extension:.js=.cjs --outdir=dist/main src/main/index.ts",
    "build:vite": "vite build",
    "build:tsc": "tsc --noEmit",
    "clean": "shx rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/node": "^16.3.2",
    "@types/react": "^17.0.14",
    "@types/react-dom": "^17.0.9",
    "@vitejs/plugin-react-refresh": "^1.3.5",
    "chalk": "^4.1.1",
    "chokidar": "^3.5.2",
    "electron": "^13.1.6",
    "esbuild": "^0.12.15",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.3",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5",
    "vite": "^2.4.2"
  }
}
